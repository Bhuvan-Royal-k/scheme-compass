'use client'; import {useState} from 'react'; import Link from 'next/link';
export default function Calculator(){const[amount,setAmount]=useState(500000),[rate,setRate]=useState(8),[years,setYears]=useState(5);const r=rate/1200,n=years*12,emi=Math.round(amount*r*Math.pow(1+r,n)/(Math.pow(1+r,n)-1));return <><header className="top"><div className="container nav"><Link className="logo" href="/">Scheme <span>Compass</span></Link></div></header><main className="formwrap"><div className="formbox"><h1>Estimated EMI Calculator</h1><p className="muted">For planning only. Actual terms depend on sanction.</p><div className="field"><label>Loan amount</label><input type="number" value={amount} onChange={e=>setAmount(+e.target.value)}/></div><div className="field"><label>Interest rate (%)</label><input type="number" value={rate} onChange={e=>setRate(+e.target.value)}/></div><div className="field"><label>Tenure (years)</label><input type="number" value={years} onChange={e=>setYears(+e.target.value)}/></div><div className="result"><span className="muted">Estimated monthly EMI</span><h2>₹{emi.toLocaleString('en-IN')}</h2></div><Link className="btn" href="/partners">Find Eligible Partners →</Link></div></main></>}
